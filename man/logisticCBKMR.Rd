% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logisticCBKMR.R
\name{logisticCBKMR}
\alias{logisticCBKMR}
\title{CBKMR for logistic regression (with logit link) without additional covariates}
\usage{
logisticCBKMR(y, Z, nsim = 5000, verbose = TRUE, thres = 10, beta0_scheme)
}
\arguments{
\item{y}{An N-dimensional vector of binary responses (0/1)}

\item{Z}{An N x p matrix of features of interest, must be standardized}

\item{nsim}{Number of MCMC iterations}

\item{verbose}{Logical, whether to print progress updates}

\item{thres}{Threshold for numerical stability in CDF calculations}

\item{beta0_scheme}{Scheme for updating beta0: 1 = Metropolis-Hastings,
2 = Elliptical Slice Sampling, 3 = Discrete Grid}
}
\value{
A list containing MCMC samples and acceptance rates
}
\description{
CBKMR for logistic regression (with logit link) without additional covariates
}
\examples{
\dontrun{
# Simulate some data
set.seed(123)
N <- 100
p <- 10
Z <- matrix(rnorm(N * p), nrow = N, ncol = p)
Z_scaled <- scale(Z)
true_w <- c(rep(1, 5), rep(0, 5))
K <- exp(-as.matrix(dist(Z_scaled))^2 \%*\% diag(true_w))
eta <- rep(0, N)
probs <- 1 / (1 + exp(-eta))
y <- rbinom(N, size = 1, prob = probs)

# Run CBKMR
result <- logisticCBKMR(y, Z_scaled, nsim = 1000, beta0_scheme = 1)
}
}
