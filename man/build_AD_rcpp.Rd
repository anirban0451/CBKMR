% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{build_AD_rcpp}
\alias{build_AD_rcpp}
\title{Build the A and D matrices for the NNGP model such that
Cov = (I - A)^T D (I - A)}
\usage{
build_AD_rcpp(coords, neighbor_matrix, sigma2, nugget_stabilizer = 1e-08)
}
\arguments{
\item{coords}{An n x p matrix of coordinates}

\item{neighbor_matrix}{An n x
m integer matrix of neighbor indices (1-based, with NA for missing neighbors)}

\item{sigma2}{The variance parameter for the Gaussian kernel}

\item{nugget_stabilizer}{A small value added to the diagonal for numerical stability}
}
\value{
A list containing the sparse matrix triplet (A_i, A_j, A_x) for the sparse matrix A
and the vector D
@examples
\dontrun{
coords <- matrix(runif(100), nrow=20)  # 20 points in 5D
neighbor_matrix <- matrix(sample(c(1:20, NA), 60, replace=TRUE), nrow=20)  # 3 neighbors per point
sigma2 <- 1.0
result <- build_AD_rcpp(coords, neighbor_matrix, sigma2)
A_i <- result$A_i
A_j <- result$A_j
A_x <- result$A_x
D <- result$D
}
}
\description{
Build the A and D matrices for the NNGP model such that
Cov = (I - A)^T D (I - A)
}
