% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rnngp_loglik_tauonly}
\alias{rnngp_loglik_tauonly}
\title{Compute the log-likelihood of the NNGP model with with tau as the shrinkage parameter}
\usage{
rnngp_loglik_tauonly(x, coords, w, tau, neighbor_matrix, nugget = 1e-08)
}
\arguments{
\item{x}{An n-dimensional vector of observations}

\item{coords}{An n x p matrix of coordinates}

\item{w}{A p-dimensional row vector of weights for each dimension}

\item{tau}{The shrinkage parameter (between 0 and 1)}

\item{neighbor_matrix}{An n x m integer matrix of neighbor indices (1-based, with NA for missing neighbors)}

\item{nugget}{A small value added to the diagonal for numerical stability}
}
\value{
The log-likelihood value
}
\description{
Compute the log-likelihood of the NNGP model with with tau as the shrinkage parameter
}
\examples{
\dontrun{
x <- rnorm(20)  # 20 observations
coords <- matrix(runif(100), nrow=20)  # 20 points in 5D
w <- runif(5)  # weights for each dimension
tau <- 0.5
neighbor_matrix <- matrix(sample(c(1:20, NA), 60, replace=TRUE), nrow=20)  # 3 neighbors per point
loglik <- rnngp_loglik_tauonly(x, coords, w, tau, neighbor_matrix)
}
}
